const filepath = process.env['CURRENT_MD_FILEPATH'] || '';
const selectedText = process.env['CURRENT_MD_SELECTED_TEXT'] || '';

// eslint-disable-next-line no-console
console.log(filepath);
// eslint-disable-next-line no-console
console.log(selectedText);
